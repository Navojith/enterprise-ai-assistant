"""RBAC-aware tool registry, `knowledge_search`, `python_analysis` and the MCP client/tools.
Built in Cycle 4 — see `registry.py`'s module docstring for the two-layer authorization model
and `factory.py` for how every tool below is assembled into one `ToolRegistry`."""
