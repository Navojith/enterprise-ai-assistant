"""FastMCP-shaped server exposing dummy employee directory, service catalog and incident
records over Streamable HTTP. Built in Cycle 4 — see `mcp_server/server.py`'s module docstring
for why this is `mcp.server.mcpserver.MCPServer`, not the `mcp.server.fastmcp.FastMCP` name a
"FastMCP" shorthand suggests in an `mcp==2.1.1` install."""
