# Development-only tooling: linting, type-checking, and the test suite. Install alongside
# requirements.txt (docs/SETUP.md); never imported by application code.
-r requirements.txt

ruff==0.16.6
mypy==2.3.1
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-httpx==0.36.2
freezegun==1.5.5
